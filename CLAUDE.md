# CLAUDE.md

## Financial Calculation Safety

- 이 서비스의 계산 결과는 사용자의 자산 판단에 영향을 준다.
- 재무 계산식, 배점, 임계값, 경제 가정은 사용자 승인 없이 변경하지 않는다.
- 불명확한 재무 기준을 추정하거나 새로 만들지 않는다.
- 외부 공식기준과 JM 자체 기준을 구분한다.
- rawValue는 중간 반올림 없이 유지한다.
- score는 rawValue로 판정한다.
- 화면에는 소수점 첫째 자리의 displayValue만 표시한다.
- displayValue를 이용해 점수나 등급을 다시 판정하지 않는다.
- 모든 점수구간은 연속적이고 상호 배타적이어야 한다.
- 구간 미매칭을 마지막 구간으로 자동 처리하지 않는다.
- 분모 0은 0%로 간주하지 않고 산출 불가로 처리한다.
- 금액 입력에서 음수, NaN, Infinity를 허용하지 않는다.
- 같은 자산·소득·저축·연금을 두 번 합산하지 않는다.
- 계산 코드 변경 전 실패하는 테스트를 먼저 작성한다.
- 모든 임계값은 T-0.01, T, T+0.01을 테스트한다.
- 계산 명세와 코드가 충돌하면 작업을 중단하고 사용자에게 질문한다.
- 관련 없는 리팩터링과 디자인 변경을 함께 수행하지 않는다.

### 미래재무 전망 불변 규칙

- 미래재무 전망 지표의 공식 명칭은 `연금소득 기준 생활비 충당률`이다.
- 이 지표는 전체 자산을 포함한 종합 은퇴 준비도가 아니다.
- 분자는 목표 나이에 실제 수령 중인 국민연금·퇴직연금·개인연금의 월소득이다.
- 분모는 승인된 물가상승률을 적용한 목표 나이의 예상 월 생활비다.
- 전체 자산과 일시금은 분자에 포함하지 않는다.
- 연금 개시 전, 종료 후, 일시금 선택은 월소득 0원이다.
- 개시 또는 종료 시점을 확인할 수 없으면 임의로 추정하거나 0원으로 처리하지 않는다.
- 필요한 연금정보가 불명확하면 충당률을 `산출 불가`로 처리한다.
- 확인 가능한 연금만 합산한 값을 완전한 충당률처럼 표시하지 않는다.
- 부분 충당률은 제공하지 않으며, 시점 정보가 불명확한 목표 나이는 전체를 `산출 불가`로 처리한다.
- 연금 활성기간은 `시작 나이 ≤ 목표 시점의 해당 인물 나이 < 종료 나이`로 판정한다.
- 국민연금은 본인과 배우자를 별도로 개시·종료 판정한 뒤 합산한다.
- 퇴직연금·개인연금의 월 수령 방식에는 수령 시작 나이가 필수다.
- 브라우저 자동 합계는 계산에 사용하지 않고 서버가 세부 입력으로 재계산한다.
- canonical 물가상승률, 개인·퇴직연금 증가율, 시작 나이 대체 규칙은 별도 승인 없이 변경하지 않는다.
- 이미 지난 60·70·80세 목표 나이의 표시 정책은 현행을 유지한다.

상세 계산 기준과 승인 대기 항목은 [미래재무 전망 계산 명세](docs/future-finance-spec.md)를 따른다.
