# Shared components

This React/Vite app primarily composes page-specific report components. Shared primitives are implemented with semantic HTML and `src/styles/app.css`; no separate reusable UI-primitives directory is present.
